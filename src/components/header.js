import React from 'react';

export default class Header extends React.Component {
    // This render function is creating the h1 component
    render() {
        return (
        <h1> React To-Do's</h1>
        );
    }
}